name: __SMOKE_OK
'on':
  workflow_dispatch: {}
  schedule:
    - cron: '*/5 * * * *'
  workflow_call: {}
jobs:
  ping:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "smoke ok"
permissions:
  contents: write
