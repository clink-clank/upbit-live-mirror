name: Mirror Live Core (legacy, disabled)
on:
  workflow_call: {}
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "legacy core disabled; use mirror_live_v20.yml"
